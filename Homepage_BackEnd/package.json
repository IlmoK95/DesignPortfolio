{
  "name": "homepage_backend",
  "version": "1.0.0",
  "description": "backend for Ilmo's website",
  "main": "index.js",
  "scripts": {
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd C:/Users/IlmoK/OneDrive/coding/homepage && npm run build && @powershell Copy-Item dist -Recurse 'C:/Users/IlmoK/OneDrive/coding/Homepage_BackEnd'",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js"
  },
  "author": "IlmoK",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.2",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.0",
    "nodemon": "^3.0.2"
  }
}
